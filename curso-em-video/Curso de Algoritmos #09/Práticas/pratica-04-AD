Algoritmo "soma-de-10-numeros"
// Disciplina   : [Linguagem e Lógica de Programação]
// Professor   : Gustavo Guanabara
// Descrição   : Algoritmo que leia 10 números e some-os. ADICIONAL: mostrar qual foi o maior múmero digitado
// Autor(a)    : Isac Raiundo
// Data atual  : 14/01/2021
Var
   contador, N, soma, M : inteiro
Inicio
   contador <- 1
   
   enquanto (contador <= 10) faca
      escreva("Escreva o", contador, "º número: ")
      leia (N)

      contador <- contador + 1
      soma <- soma + N

      se (N > M) entao 
         M <- N
      fimSe
   fimEnquanto
   
   escrevaL ("A soma de todos os números é", soma)
Fimalgoritmo